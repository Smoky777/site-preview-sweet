

# Plan: "Under Construction" Page (English)

## Single file change

**`src/pages/Index.tsx`** — Full rewrite:
- Full-screen Unsplash building/skyline photo as background with dark overlay
- "Caruso Capital" branding
- "Under Construction" heading
- "We're building something exceptional" subtitle
- 30-day countdown timer (Days / Hours / Minutes / Seconds)
- Email notification form with toast confirmation
- Social media icon links (LinkedIn, Instagram, Twitter)
- Clean, elegant dark theme with fade-in animations

**`src/index.css`** — Add `fade-in` and `pulse-glow` keyframe animations

No new dependencies needed — uses existing `lucide-react`, `Button`, `Input`, and toast.

